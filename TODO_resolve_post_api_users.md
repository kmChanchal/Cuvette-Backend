# TODO: Resolve "Cannot POST /api/users" Error

- [x] Add a users array to JSONApiServer.js similar to the products array
- [x] Add POST /api/users route handling in JSONApiServer.js to parse request body, assign an ID, add to users array, and return the new user
- [x] Ensure CORS headers allow POST method for /api/users
- [x] Test the server by running it and making a POST request to /api/users
